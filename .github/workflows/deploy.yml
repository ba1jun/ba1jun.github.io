name: Build, Test, and Deploy Site

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY_IMAGE: erfianugrah/revista-4
  # Keep the template author's external deployment targets disabled. GitHub
  # Pages is the only production target for this repository.
  ENABLE_UPSTREAM_DEPLOYS: "false"

permissions:
  contents: read

jobs:
  build-revista:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Setup Bun environment
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install libavif-bin (required for astro-image-hq encoder)
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends libavif-bin
          avifenc --version

      - name: Cache Bun packages
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-cache-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-cache-

      - name: Cache Astro image assets
        uses: actions/cache@v4
        with:
          path: node_modules/.astro
          key: ${{ runner.os }}-astro-assets-${{ hashFiles('bun.lock') }}-${{ hashFiles('src/content/**/*.mdx', 'src/content/**/*.md') }}
          restore-keys: |
            ${{ runner.os }}-astro-assets-${{ hashFiles('bun.lock') }}-

      - name: Install project dependencies
        run: bun install

      - name: Build project with retry
        env:
          MAX_ATTEMPTS: 3
          RETRY_INTERVAL: 30
        run: |
          for attempt in $(seq 1 $MAX_ATTEMPTS); do
            if bun run build; then
              echo "Build succeeded on attempt $attempt."
              exit 0
            fi
            if [ "$attempt" -lt "$MAX_ATTEMPTS" ]; then
              echo "Build attempt $attempt failed. Retrying in $RETRY_INTERVAL seconds..."
              sleep $RETRY_INTERVAL
            fi
          done
          echo "Build failed after $MAX_ATTEMPTS attempts."
          exit 1

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist
          retention-days: 7

  # Quality gate: the built artifact is what gets deployed, so test IT.
  # (deploy jobs all `needs` this job.)
  test:
    needs: build-revista
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Setup Bun environment
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Cache Bun packages
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-cache-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-cache-

      - name: Install project dependencies
        run: bun install

      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: dist
          path: dist

      - name: Install Playwright chromium
        run: bunx playwright install --with-deps chromium

      - name: Run Playwright tests
        run: bunx playwright test

      - name: Upload Playwright failure screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-screenshots
          path: test-results
          retention-days: 7

  deploy-to-github-pages:
    needs: [build-revista, test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    concurrency:
      group: github-pages
      cancel-in-progress: false
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Download built site
        uses: actions/download-artifact@v5
        with:
          name: dist
          path: dist

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  deploy-to-cloudflare-workers:
    needs: [build-revista, test]
    if: github.event_name == 'push' && env.ENABLE_UPSTREAM_DEPLOYS == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "24"

      - name: Setup Bun environment
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.10"

      - name: Install dependencies
        run: bun install

      - name: Download built artifacts
        uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist

      - name: Deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_WRANGLER_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy
          packageManager: bun
          wranglerVersion: "4.75.0"

  build-and-push-docker:
    needs: [build-revista, test]
    if: github.event_name != 'pull_request' && env.ENABLE_UPSTREAM_DEPLOYS == 'true'
    runs-on: ubuntu-latest
    outputs:
      digest: ${{ steps.build.outputs.digest }}
      tags: ${{ steps.meta.outputs.tags }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_REGISTRY_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY_IMAGE }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64,linux/arm/v6,linux/arm/v7
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: true
          sbom: true

  inspect-docker-image:
    needs: build-and-push-docker
    runs-on: ubuntu-latest
    steps:
      - name: Inspect Docker image
        run: |
          echo "Inspecting image digest: ${{ needs.build-and-push-docker.outputs.digest }}"
          docker buildx imagetools inspect ${{ env.REGISTRY_IMAGE }}@${{ needs.build-and-push-docker.outputs.digest }}

  sign-docker-image:
    needs: [build-and-push-docker, inspect-docker-image]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write # Needed for keyless signing
    steps:
      - name: Install Cosign
        uses: sigstore/cosign-installer@v4.1.2
        with:
          cosign-release: "v2.4.1"

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_REGISTRY_TOKEN }}

      - name: Sign the Docker image
        env:
          COSIGN_EXPERIMENTAL: 1
          IMAGE_DIGEST: ${{ needs.build-and-push-docker.outputs.digest }}
          REPOSITORY: ${{ github.repository }}
        run: |
          IMAGE_REF="${REGISTRY_IMAGE}@${IMAGE_DIGEST}"

          # Check if image is already signed
          if cosign verify "${IMAGE_REF}" \
            --certificate-identity-regexp="https://github.com/${REPOSITORY}/.*" \
            --certificate-oidc-issuer=https://token.actions.githubusercontent.com 2>/dev/null; then
            echo "Image is already signed and verified. Skipping signing."
            exit 0
          fi

          # Attempt to sign with retry logic
          MAX_ATTEMPTS=3
          RETRY_DELAY=10
          attempt=1

          while [ $attempt -le $MAX_ATTEMPTS ]; do
            if cosign sign --yes "${IMAGE_REF}"; then
              echo "Successfully signed image on attempt $attempt"
              exit 0
            fi

            if [ $attempt -lt $MAX_ATTEMPTS ]; then
              echo "Signing attempt $attempt failed. Retrying in $RETRY_DELAY seconds..."
              sleep $RETRY_DELAY
              RETRY_DELAY=$((RETRY_DELAY * 2))
            fi
            attempt=$((attempt + 1))
          done

          echo "Warning: Signing failed after $MAX_ATTEMPTS attempts."
          echo "The image is still available and usable, but without a signature."
          exit 0

      - name: Verify Docker image signature
        continue-on-error: true
        env:
          COSIGN_EXPERIMENTAL: 1
          IMAGE_DIGEST: ${{ needs.build-and-push-docker.outputs.digest }}
          REPOSITORY: ${{ github.repository }}
        run: |
          cosign verify "${REGISTRY_IMAGE}@${IMAGE_DIGEST}" \
            --certificate-identity-regexp="https://github.com/${REPOSITORY}/.*" \
            --certificate-oidc-issuer=https://token.actions.githubusercontent.com
